conky.config = {
  out_to_console = true,
  out_to_x = false,
  background = false,
  update_interval = 1,
  total_run_times = 0,
  use_spacer = 'none',
};

conky.text = [[
  ${cpu}% ::: $memperc% ($mem) ::: ${downspeed enp4s0}K/s ${upspeed enp4s0}K/s ::: ${time %a %b %d %I:%M%P}
]];

